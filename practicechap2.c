// practice ques 1 to check if the given input is even or odd
// even -> 1 
// odd -> 0

#include<stdio.h>
int main(){
    char stars ='*'; // we always use one chracter as it will be invalid to use two characters at the same tine with char function
    int a;
    int x ; int y = x ; // valid ( if we had not used semi coulen so it would be a invalid statement)
    printf("enter a number");
    scanf("%d",&a);
    printf(" %d", a %2 == 0);
 printf("%d", 8^8);

    
    

    return 0;
}